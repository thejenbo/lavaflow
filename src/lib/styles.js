const COLOR_PRIMARY = '#ec008c';
const COLOR_SECONDARY = '#fc6767';
const COLOR_GREY = '#2f4858';

const GRADIENT = `linear-gradient(to bottom, ${COLOR_SECONDARY}, ${COLOR_PRIMARY})`;

const BOX_SHADOW = `linear-gradient(to bottom, ${COLOR_SECONDARY}, ${COLOR_PRIMARY})`;

const FONT_DISPLAY= '"Montserrat Alternates", sans-serf';
const FONT_PARAGRAPH = '"Montserrat", sans-serf';

const BREAKPOINT_MD = '768px';
